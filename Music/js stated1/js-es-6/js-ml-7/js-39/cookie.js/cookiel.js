const getCookie = name=>{
    const cookei = document.cookei;
    const allCookie = cookei.split(';')
    const findCookkei = allCookie.find(c=>c.includes(name));
    if(findCookkei){
        'country'
        const cooknameValue = findCookkei.split('=');
        return cooknameValue;
    }
    
}

// return thgiens is most open banlgadehislsitej
